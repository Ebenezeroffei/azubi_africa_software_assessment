import CheckoutPageWrapper from "@/components/products/CheckoutPageWrapper";
import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Checkout"
}

export default CheckoutPageWrapper;