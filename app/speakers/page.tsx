import SpeakersPageWrapper from "@/components/products/SpeakersPageWrapper";
import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Speakers"
}

export default SpeakersPageWrapper;